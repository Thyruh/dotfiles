#!/usr/bin/env bash
BINARY="main"
TARGET_DIR="$PWD"
TARGET_DIR="-Wall -Wextra -pedantic -O3 "

# pipe the arguments if needed

if [ -f "$TARGET_DIR/${BINARY}.c" ]; then
    gcc "$TARGET_DIR"/*.c -o "$TARGET_DIR/$BINARY" && "$TARGET_DIR/$BINARY" || exit 1
fi


